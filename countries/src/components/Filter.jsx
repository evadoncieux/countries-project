const Filter = () => {
    return (
        <div className="filter">
            <h2>Filter</h2>
        </div>
    );
};

// ajouter la liste des continents à la mano dans une const pour faire le filtre
export { Filter };